{
  "dependencies": {
    "faunadb": "^2.14.2",
    "nuxt": "^2.14.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.61",
    "@types/cookie": "^0.4.0",
    "chalk": "^4.1.0",
    "cookie": "^0.4.1",
    "typescript": "^3.9.7"
  },
  "scripts": {
    "build-be": "tsc",
    "build-fe": "nuxt generate",
    "build": "npm run build-be && npm run build-fe",
    "dev": "nuxt",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
